--extra-index-url https://download.pytorch.org/whl/cu128

gradio>=5.0.0,<6.0.0
# Current target system: NVIDIA RTX 4090 + CUDA 12.8.
# Use the GPU runtime by default so Docker/local installs do not silently fall
# back to CPU-only wheels.
onnxruntime-gpu>=1.20.0
numpy>=1.26.0
pillow>=10.0.0
huggingface_hub>=0.34.0
opencv-python>=4.9.0

# PowerPaint v2-1 (iopaint vendored BrushNet pipeline)
# iopaint pins old versions of diffusers/peft/gradio/hub that conflict with
# our gradio 5.x + hub 1.x stack, so install with --no-deps and supply
# compatible deps manually.
iopaint==1.6.0  # install with: pip install --no-deps iopaint==1.6.0
# CUDA-enabled PyTorch wheels are resolved via the extra index above.
torch>=2.8,<2.11
torchvision
diffusers>=0.32,<0.36
transformers>=4.39,<5
tokenizers>=0.22
accelerate>=1.0
safetensors>=0.4
peft>=0.17
controlnet-aux==0.0.3
timm
einops
omegaconf
antlr4-python3-runtime==4.9.3
scikit-image
scipy
loguru
yacs
easydict
piexif==1.1.3
python-socketio==5.7.2
typer-config==1.4.0
psutil
ftfy
sentencepiece
requests
standard-imghdr; python_version >= "3.13"
